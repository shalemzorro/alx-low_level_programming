#include <stdio.h>

/**
 * main - Prints all single digits from 0 to 9
 *
 * Return: Always (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("i%, i);
	}

	printf('\n');

	return (0);
}
